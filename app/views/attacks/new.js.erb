$('.navbar-collapse').collapse("hide");
$('#page_content').promise().done(function(){
    document.getElementById('page_content').innerHTML = "";
    $("#page_content").html("<%= escape_javascript(render partial: 'new', locals: { :remote => true } ) %>");
});
