<%= form_for(@webhost) do |f| %>
  <% if @webhost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@webhost.errors.count, "error") %> prohibited this webhost from being saved:</h2>

      <ul>
      <% @webhost.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :attackID %><br>
    <%= f.text_field :attackID %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
